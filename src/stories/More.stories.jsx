import React from 'react';
import { More } from '../components/Button';

export default {
	title: 'Atoms/Button/More',
	component: More,
};

const Template = (args) => <More {...args} />;

export const index = Template.bind({});
index.args = {};
